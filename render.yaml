services:
  - type: web
    name: titanium-ai-saas
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn production_stack.backend.app:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.0
